sudo: required

language: bash

services:
  - docker

env:
  matrix:
    - OS_TYPE=debian OS_VERSION=9 BUILD_TYPE=arm-linux-gnueabi

script:
  - bash ./tests/run_docker_test.sh ${OS_TYPE} ${OS_VERSION} ${BUILD_TYPE}
